## 1. UI Components

- [x] 1.1 Create the mock data structure / interface for the Services table (e.g., `src/types/service.ts` or inline in the component).
- [x] 1.2 Create the `ServicesTable` component using existing Shadcn UI table patterns (e.g., `src/components/services/services-table.tsx`).

## 2. Page & Routing

- [x] 2.1 Create the new page layout for `/services` (e.g., `src/app/(dashboard)/services/page.tsx`).
- [x] 2.2 Integrate the `ServicesTable` component into the new page.

## 3. Navigation

- [x] 3.1 Update the main sidebar/navigation component to include a link to the `/services` route.
