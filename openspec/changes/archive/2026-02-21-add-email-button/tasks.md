## 1. UI Components Setup

- [x] 1.1 Create `AddEmailButton` component that includes a Dialog/Modal trigger.
- [x] 1.2 Design the modal form UI for adding a new Master Account Email (fields: Email address at minimum).
- [x] 1.3 Ensure the button styling matches the dashboard theme (e.g., using `Button` with a `Plus` icon).

## 2. Integration with Emails Page

- [x] 2.1 Import the `AddEmailButton` into `src/app/(dashboard)/emails/page.tsx`.
- [x] 2.2 Place the button logically above the `DataTable` component.
- [x] 2.3 Verify responsive design (ensure it looks good on mobile and desktop views).
