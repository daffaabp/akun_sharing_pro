# Tasks for Allow Single-Seat Pools

## Implementation
- [x] Update `src/components/pools/create-pool-dialog.tsx` to set `min={1}` on the target seats input.

## Verification
- [x] Verify the UI allows creating a 1-seat pool.
- [x] Ensure no TypeScript or build errors are introduced.
