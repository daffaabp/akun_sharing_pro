# Spec: pool-actions

## Requirements

### Requirement: Create Pool Support Single Seat
The Create Pool dialog MUST allow entering `1` in the Target Seats input field to support single-seat (private) pools.
The minimum allowed value for the Target Seats HTML input MUST be `1`.
